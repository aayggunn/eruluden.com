function mesajGonder() {
    var mesajMetni = document.getElementById("mesaj").value;

    // Mesajı sunucuya gönderme işlemi burada gerçekleştirilebilir

    // Mesaj gönderildikten sonra metin kutusunu temizle
    document.getElementById("mesaj").value = "";

    // Gönderilen mesajı sohbet penceresine eklemek için gerekli kod buraya eklenebilir
}
